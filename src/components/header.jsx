import React from "react";
import Timer from "./timer";
import "./header.css";

const Header = () => {
  return (
    <header className="header">
      <Timer />
    </header>
  );
};

export default Header;
